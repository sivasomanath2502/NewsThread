# NewsAPI key — get a free one at https://newsapi.org
# The server reads this to call NewsAPI — never exposed to the browser
NEWS_API_KEY=your_newsapi_key_here

# Optional: Express API server port for local dev (default 8787)
#PORT=8787
